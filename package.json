{
  "name": "idea-pool-vc",
  "version": "1.0.0",
  "description": "Idea pooling and tagging project",
  "scripts": {
    "build:hooks-rs": "cd .claude/hooks-rs && cargo build --release && cp target/release/validate-command.exe ../validate-command.exe"
  }
}
